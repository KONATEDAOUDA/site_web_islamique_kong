{% extends 'frontend/base.html.twig' %} 

{% block title %}Email envoyé - Portail Islamique de Kong{% endblock %}

{% block main %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow">
                <div class="card-header bg-success text-white text-center">
                    <h2 class="mb-0">Email envoyé</h2>
                </div>
                <div class="card-body">
                    <p>Un email contenant un lien pour réinitialiser votre mot de passe a été envoyé à l'adresse email fournie.</p>
                    <p>Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
                    <p>Si vous ne recevez pas d'email, veuillez vérifier votre dossier spam ou <a href="{{ path('app_forgot_password_request') }}">réessayer</a>.</p>
                    
                    <div class="text-center mt-4">
                        <a href="{{ path('app_login') }}" class="btn btn-primary">Retour à la connexion</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}